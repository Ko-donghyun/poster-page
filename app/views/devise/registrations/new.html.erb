<br><br>
  <div class="col-lg-3 col-md-2 col-sm-2 col-xs-0"></div>
  <div class="col-lg-6 col-md-8 col-sm-8 col-xs-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">회원 가입</h3>
      </div>
    
      <div class="panel-body">
 
        
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= devise_error_messages! %>
        
          <br>
          <div class="field">
            <%= f.email_field :email, autofocus: true, class:"form-control", placeholder:"이메일" %>
          </div>
          <br>
          
          <div class="field">
            <% if @minimum_password_length %>
            <em>(비밀번호는 <%= @minimum_password_length %> 글자 이상 입력해주세요)</em> 
            <% end %><br />
            <%= f.password_field :password, autocomplete: "off", class:"form-control", placeholder:"비밀번호" %>
          </div>
          <br>
        
          
          <div class="field">
            <%= f.password_field :password_confirmation, autocomplete: "off", class:"form-control", placeholder:"비밀번호 재확인" %>
          </div>
          
          <br>
          <div class="action">
            <%= f.submit "회원가입", class:"btn btn-primary" %>
          </div>
          <br>
          
        <% end %>
        
        
      </div>
    </div>
  <div class="col-lg-3 col-md-2 col-sm-2 col-xs-0"></div>
</div>